/**
 * Shared constants used across the application
 */

/**
 * Warning message displayed when embeddings are running in CPU-only mode
 */
export const CPU_MODE_WARNING = 'WARNING: Embeddings running in CPU-only mode - this will take a very long time. For 10-100x faster processing, add OPENAI_API_KEY to your environment configuration.';
